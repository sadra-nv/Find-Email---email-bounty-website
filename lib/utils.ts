// export function generateDots(count: number, ctx: CanvasRenderingContext2D) {
//   for (let i = 0; i < count; i++) {
//     const x = Math.random() * ctx.canvas.width;
//     const y = Math.random() * ctx.canvas.height;
//     const radius = 1;

//     ctx.beginPath();
//     ctx.arc(x, y, radius, 0, Math.PI * 2);
//     ctx.fillStyle = "white";
//     ctx.fill();
//   }
// }
